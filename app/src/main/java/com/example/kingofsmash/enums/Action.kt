package com.example.kingofsmash.enums

enum class Action {
    THROW_DICES,
    EXECUTE_DICES,
    DF_ATTACKED,
    CHECK_DF,
    CHECK_GAME_OVER,
    CARD_SELECTION,
    EXECUTE_CARDS,
    CHECK_GAME_OVER_AFTER_CARDS,
    WAIT_END_TURN,
}